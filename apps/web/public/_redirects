# Redirect old vendor paths to new plural vendors path
/vendor/*    /vendors/:splat    301

# SPA Fallbacks for Dashboard and Admin routes (handled by Next.js but kept for safety)
/dashboard/*  /dashboard/index.html  200
/listings/*   /listings/index.html   200
/leads/*      /leads/index.html      200
/subscription/* /subscription/index.html 200
/settings/*   /settings/index.html   200
/admin/*     /admin/:splat.html     200

# Next.js handles its own dynamic routing now that we are using SSR (no output: export)
